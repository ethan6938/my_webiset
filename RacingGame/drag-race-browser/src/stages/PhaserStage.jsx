import React,{useEffect} from "react";import useDragRaceGame from "../hooks/useDragRaceGame";export default function PhaserStage({onReady,children,...hookProps}){const{mountRef,gameRef,createGame}=useDragRaceGame(hookProps);useEffect(()=>{createGame();onReady?.(gameRef.current);},[]);return(<><div ref={mountRef} className="block" style={{width:"100vw",height:"100vh"}}/>{children}</>);}